<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- App has no ads and no ad personalization/attribution; strip the ad-related
         permissions that play-services-measurement (Firebase Analytics) merges in by default. -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />

    <application
        android:name="com.nomnomsom.armstrongandgetty.AGPodcastApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Armstrong &amp; Getty Podcast"
        android:supportsRtl="true"
        android:theme="@style/Theme.AGPodcast"
        tools:targetApi="36">

        <activity
            android:name="com.nomnomsom.armstrongandgetty.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AGPodcast">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Media playback service: background audio, lock screen, notifications, AND Android Auto -->
        <service
            android:name="com.nomnomsom.armstrongandgetty.media.PlaybackService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
            </intent-filter>
        </service>

        <!-- Android Auto: declare this as a media app -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <!-- Media button receiver: required for playback resumption. Without a manifest
             MEDIA_BUTTON receiver Media3 reports canResumePlaybackOnStart()=false, so System UI
             and Android Auto's "resume last session" probe (MediaResumeListener) fail to connect.
             Media3 routes the button/resumption intent to PlaybackService, which serves the last
             in-progress episode via MediaLibrarySession.Callback.onPlaybackResumption. -->
        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- Chromecast: Options Provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="com.nomnomsom.armstrongandgetty.media.CastOptionsProvider" />

        <!-- Chromecast: Media Transfer Receiver -->
        <receiver android:name="androidx.mediarouter.media.MediaTransferReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
